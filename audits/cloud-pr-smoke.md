# Cloud PR Smoke — Orientation

Smoke test of the cloud dispatch → commit path. Scope: read CLAUDE.md, write this file, commit. No tests run, no further exploration.

- **What this is:** TenSixtyThree (aka 1063 / 10-63) — a local-first Next.js 16 dashboard with an AI dispatcher ("the Overseer") that manages Claude Code sessions across multiple projects: fleet health, knowledge harvesting, session feedback loop, dispatch outcome tracking, briefings, and a backend-only Teams/collision-plane foundation.
- **Stack & entry points:** TypeScript strict, App Router, Tailwind 4, Prisma + Vitest/Playwright. The load-bearing modules are `lib/claude-dispatcher.ts` (session dispatch), `lib/health-engine.ts` + `lib/progress-engine.ts` (scoring), `lib/escalation-detector.ts` (log tag parsing), and the `app/api/overseer/chat/` + `app/api/webhook/session-complete/` routes.
- **How work flows:** every request follows **Prime → Plan → Red → Green → Validate** — read the request file in `requests/` and `.claude/handoff.md`, write failing tests from each acceptance criterion *first*, implement to green, then `scripts/validate.sh` (the same script CI runs). Tests are never optional; the UI-only "tests after" escape hatch still requires tests before a request closes. Commit as `type(scope): description` on a `phase-N-description` branch, and update `handoff.md` when done.
- **Watch out:** CLAUDE.md's Stack section still describes SQLite at `./dev.db`, while `.claude/rules/db.md` (Phase 51) says the database is Postgres everywhere — local dev and tests both run against the `tensixtythree-pg` container, and SQLite is retired. Trust the rules file; treat the CLAUDE.md lines as stale. [HUMAN TODO] reconcile the SQLite references in CLAUDE.md (lines 15 and 21) with the Postgres reality.
