# Request 51.2 — Better Auth: identity for the hosted control plane

## Objective
Self-hosted auth on Better Auth: GitHub OAuth (primary) + Google OAuth +
magic-link fallback. No passwords. DB-backed revocable sessions in our
Postgres. Foundation for 51.3 (Teams onto the organization plugin).

## Acceptance criteria → tests (RED first)
| # | Criterion | Test |
|---|-----------|------|
| 1 | Auth route handler mounts and serves the Better Auth API | app/api/auth/route smoke: GET session endpoint returns 200-shape for anonymous |
| 2 | Server helper `getServerSession()` returns null anonymous / session when signed in | lib/auth.test.ts with a seeded session row |
| 3 | Protected-route helper `requireSession()` rejects anonymous requests (401) | lib/auth.test.ts |
| 4 | Better Auth tables exist in the schema (user/session/account/verification) and prisma validates | schema test: prisma validate + model presence |
| 5 | Sign-in page renders GitHub + Google + magic-link options | app/signin page smoke (jsdom) |

## Justin-owned prerequisites (can trail the code; providers no-op until set)
- GitHub OAuth App (callback: http://localhost:3000/api/auth/callback/github) → GITHUB_CLIENT_ID/SECRET
- Google OAuth client (callback: .../api/auth/callback/google) → GOOGLE_CLIENT_ID/SECRET
- BETTER_AUTH_SECRET generated; domains purchased for prod URLs

## Notes
- Config verified against live docs (agent report) — never from memory.
- Auth enforcement on existing routes lands with 51.3 (single-user local mode
  must keep working until the hosted deploy — feature-flag via AUTH_REQUIRED).
