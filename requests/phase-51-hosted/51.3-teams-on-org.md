# Request 51.3 — Teams onto the organization plugin

## Objective
Replace the hand-rolled Team/Membership/Invite models with Better Auth's
Organization/Member/Invitation. lib/teams.ts keeps its seam (same exported
API) but its security-critical internals (invite tokens, acceptance) move to
plugin-owned tables. Work attribution (Dispatch/HumanTask) re-points to
organizationId. Legacy models DELETED (they were empty).

## AC → tests
| # | Criterion | Test |
|---|-----------|------|
| 1 | createTeam creates an Organization + owner Member | lib/teams.test.ts |
| 2 | inviteMember creates an Invitation (pending, 7-day expiry) | lib/teams.test.ts |
| 3 | acceptInvite creates a Member and burns the Invitation | lib/teams.test.ts |
| 4 | expired/accepted invitations are rejected | lib/teams.test.ts |
| 5 | listTeamActivity spans dispatches+tasks per organization | lib/team-activity.test.ts |
| 6 | /api/team + /api/team/invite + /team page work on org tables | route + page tests |

## Notes
- Invitation email sending: STUBBED until tensixtythree.com exists (HUMAN
  TODO: domain + Resend sending identity). Invites shareable by id/token flow.
- migrate-dev-db ORDER keeps legacy names; zero-row sources skip harmlessly.
